name: Refresh Jobs

on:
  schedule:
    - cron: '0 5 * * *'   # 8:00 AM AST (Asia/Riyadh = UTC+3)
    - cron: '0 15 * * *'  # 6:00 PM AST (Asia/Riyadh = UTC+3)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install requests
      - name: Run scraper
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
        run: python scripts/scraper.py
      - name: Commit and push updated jobs
        run: |
          git config user.name "Owais Job Bot"
          git config user.email "bot@owais-jobs.local"
          git add data/jobs.json data/seen.json
          if git diff --staged --quiet; then
            echo "No new jobs found — nothing to commit."
          else
            TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M UTC')
            git commit -m "Refresh jobs – ${TIMESTAMP}"
            git push
          fi
